# Dependencies
node_modules/
.pnpm-store/

# Build
dist/
.astro/
backend/public/
*.tsbuildinfo

# Environment & secrets
.env
.env.*
!.env.example

# Database y uploads locales (DATA_DIR=./data)
*.sqlite
*.sqlite-journal
*.sqlite-wal
*.sqlite-shm
data/

# Logs & OS
*.log
.DS_Store
._*
Thumbs.db

# IDE
.idea/
.vscode/*
!.vscode/extensions.json

# Docker: overrides locales (puertos, secrets, bind mounts)
docker-compose.override.yml

# AutoSkills (npx autoskills) — árboles generados, no el lockfile
.agents/
.claude/skills/

# Settings locales de agentes (máquina / secretos). Rules e hooks sí se commitean.
.claude/*.local.json
.claude/*.local.md
.claude/settings.local.json
.cursor/*.log

# Wrangler (Worker BFF)
.wrangler/
frontend/.dev.vars
frontend/worker-configuration.d.ts

# Caches de tooling
.vite/
.cache/
vite.config.*.timestamp-*
*.lcov

# Test / coverage
coverage/
