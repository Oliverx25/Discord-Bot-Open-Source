---
import PanelLayout from "./PanelLayout.astro";

interface Props {
  title: string;
  description?: string;
}

const { title, description } = Astro.props;
---

<PanelLayout title={title} description={description}>
  <slot />
</PanelLayout>
