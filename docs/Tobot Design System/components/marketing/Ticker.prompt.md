Marquesina de marca. Una por página, arriba del todo, con frases cortas en mayúsculas.

```jsx
<Ticker items={['One bot, one bill','18 módulos','15 gratis','Por cuenta, no por servidor']} />
```

No la uses dentro del dashboard: la única animación en bucle del sistema vive en marketing.
