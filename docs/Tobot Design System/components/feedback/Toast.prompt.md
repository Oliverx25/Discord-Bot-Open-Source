Confirms an action that already succeeded. The 2px left rule is the only place a colored edge accent appears in the system.

```jsx
<Toast tone="success" title="Auto-mod is on" description="Filters apply to new messages." onDismiss={fn} />
```

Auto-dismiss after 4s (success) or never (danger).
