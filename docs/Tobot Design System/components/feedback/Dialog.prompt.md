Confirmation and short forms only — never for content a page could hold.

```jsx
<Dialog open title="Reset the economy?" tone="danger"
  description="Every balance on this server goes back to 0. It can't be undone."
  footer={<><Button variant="ghost" onClick={close}>Keep it</Button><Button variant="destructive">Reset</Button></>} />
```
