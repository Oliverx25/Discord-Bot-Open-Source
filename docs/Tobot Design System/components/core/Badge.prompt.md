Status pill. Tones `free` and `pro` are the canonical plan markers — never restyle tiers ad hoc.

```jsx
<Badge tone="pro">Pro</Badge>
<Badge tone="success" dot>Active</Badge>
<Badge tone="warning" size="sm">Rate limited</Badge>
```

Uppercase + 700 weight by default; pass `uppercase={false}` for counts or usernames.
