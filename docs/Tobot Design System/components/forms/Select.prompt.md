Use for closed sets of 4+ options (channel, timezone, punishment). Below 4, prefer `Radio`.

```jsx
<Select label="Log channel" options={[{value:'mod',label:'#mod-log'},{value:'audit',label:'#audit'}]} />
```
