Exclusive choice, 2–4 options, all visible.

```jsx
<Radio label="On violation" name="punish" value={v} onChange={setV}
  options={[{value:'warn',label:'Warn'},{value:'mute',label:'Mute',hint:'10 minutes'},{value:'ban',label:'Ban'}]} />
```
