Applies instantly — no Save button next to it. Used on every module header in the dashboard.

```jsx
<Switch checked={on} onChange={e=>setOn(e.target.checked)} label="Auto-mod" hint="Saved automatically" />
```
