For independent booleans in a list (log events, automod filters).

```jsx
<Checkbox checked label="Message deleted" hint="Includes bulk deletes." onChange={fn} />
```

Use `Switch` instead when the toggle takes effect immediately.
